import { r as RegisterClass } from "./typeStore-DHv0TG1U.js";
import { A as FlowGraphInteger, m as RichTypeFlowGraphInteger, t as FlowGraphBlock, u as RichTypeAny } from "./flowGraphBlock-Cu7z1xvN.js";
//#region node_modules/@babylonjs/core/FlowGraph/Blocks/Data/Utils/flowGraphIndexOfBlock.js
/**
* This block takes an object as input and an array and returns the index of the object in the array.
*/
var FlowGraphIndexOfBlock = class extends FlowGraphBlock {
	/**
	* Construct a FlowGraphIndexOfBlock.
	* @param config construction parameters
	*/
	constructor(config) {
		super(config);
		this.config = config;
		this.object = this.registerDataInput("object", RichTypeAny);
		this.array = this.registerDataInput("array", RichTypeAny);
		this.index = this.registerDataOutput("index", RichTypeFlowGraphInteger, new FlowGraphInteger(-1));
	}
	/**
	* @internal
	*/
	_updateOutputs(context) {
		const object = this.object.getValue(context);
		const array = this.array.getValue(context);
		if (array) this.index.setValue(new FlowGraphInteger(array.indexOf(object)), context);
	}
	/**
	* Serializes this block
	* @param serializationObject the object to serialize to
	*/
	serialize(serializationObject) {
		super.serialize(serializationObject);
	}
	getClassName() {
		return "FlowGraphIndexOfBlock";
	}
};
RegisterClass("FlowGraphIndexOfBlock", FlowGraphIndexOfBlock);
//#endregion
export { FlowGraphIndexOfBlock as t };

//# sourceMappingURL=flowGraphIndexOfBlock-Cc-q9BbY.js.map