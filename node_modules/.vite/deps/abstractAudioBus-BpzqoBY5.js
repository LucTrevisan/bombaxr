import { n as _AudioAnalyzer, o as _GetVolumeAudioProperty, s as _GetVolumeAudioSubNode, u as AbstractNamedAudioNode } from "./webAudioBaseSubGraph-DlOLx-lf.js";
//#region node_modules/@babylonjs/core/AudioV2/abstractAudio/abstractAudioBus.js
/**
* Abstract class representing an audio bus with volume control.
*
* An audio bus is a node in the audio graph that can have multiple inputs and outputs. It is typically used to group
* sounds together and apply effects to them.
*/
var AbstractAudioBus = class extends AbstractNamedAudioNode {
	constructor(name, engine) {
		super(name, engine, 3);
		this._analyzer = null;
	}
	/**
	* The analyzer features of the bus.
	*/
	get analyzer() {
		return this._analyzer ?? (this._analyzer = new _AudioAnalyzer(this._subGraph));
	}
	/**
	* The output volume of the bus.
	*/
	get volume() {
		return _GetVolumeAudioProperty(this._subGraph, "volume");
	}
	set volume(value) {
		const node = _GetVolumeAudioSubNode(this._subGraph);
		if (!node) throw new Error("No volume subnode");
		node.volume = value;
	}
	/**
	* Releases associated resources.
	*/
	dispose() {
		super.dispose();
		this._analyzer?.dispose();
		this._analyzer = null;
		this._subGraph.dispose();
	}
};
//#endregion
export { AbstractAudioBus as t };

//# sourceMappingURL=abstractAudioBus-BpzqoBY5.js.map