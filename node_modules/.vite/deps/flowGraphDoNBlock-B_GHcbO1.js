import { r as RegisterClass } from "./typeStore-DHv0TG1U.js";
import { A as FlowGraphInteger, m as RichTypeFlowGraphInteger } from "./flowGraphBlock-Cu7z1xvN.js";
import { t as FlowGraphExecutionBlockWithOutSignal } from "./flowGraphExecutionBlockWithOutSignal-xSLd1rxK.js";
//#region node_modules/@babylonjs/core/FlowGraph/Blocks/Execution/ControlFlow/flowGraphDoNBlock.js
/**
* A block that executes a branch a set number of times.
*/
var FlowGraphDoNBlock = class extends FlowGraphExecutionBlockWithOutSignal {
	constructor(config = {}) {
		super(config);
		this.config = config;
		this.config.startIndex = config.startIndex ?? new FlowGraphInteger(0);
		this.reset = this._registerSignalInput("reset");
		this.maxExecutions = this.registerDataInput("maxExecutions", RichTypeFlowGraphInteger);
		this.executionCount = this.registerDataOutput("executionCount", RichTypeFlowGraphInteger, new FlowGraphInteger(0));
	}
	_execute(context, callingSignal) {
		if (callingSignal === this.reset) this.executionCount.setValue(this.config.startIndex, context);
		else {
			const currentCountValue = this.executionCount.getValue(context);
			if (currentCountValue.value < this.maxExecutions.getValue(context).value) {
				this.executionCount.setValue(new FlowGraphInteger(currentCountValue.value + 1), context);
				this.out._activateSignal(context);
			}
		}
	}
	/**
	* @returns class name of the block.
	*/
	getClassName() {
		return "FlowGraphDoNBlock";
	}
};
RegisterClass("FlowGraphDoNBlock", FlowGraphDoNBlock);
//#endregion
export { FlowGraphDoNBlock as t };

//# sourceMappingURL=flowGraphDoNBlock-B_GHcbO1.js.map