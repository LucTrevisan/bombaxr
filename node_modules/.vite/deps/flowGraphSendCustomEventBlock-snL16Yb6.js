import { r as RegisterClass } from "./typeStore-DHv0TG1U.js";
import { t as FlowGraphExecutionBlockWithOutSignal } from "./flowGraphExecutionBlockWithOutSignal-xSLd1rxK.js";
//#region node_modules/@babylonjs/core/FlowGraph/Blocks/Event/flowGraphSendCustomEventBlock.js
/**
* A block that sends a custom event.
* To receive this event you need to use the ReceiveCustomEvent block.
* This block has no output, but does have inputs based on the eventData from the configuration.
* @see FlowGraphReceiveCustomEventBlock
*/
var FlowGraphSendCustomEventBlock = class extends FlowGraphExecutionBlockWithOutSignal {
	constructor(config) {
		super(config);
		this.config = config;
		for (const key in this.config.eventData) this.registerDataInput(key, this.config.eventData[key].type, this.config.eventData[key].value);
	}
	_execute(context) {
		const eventId = this.config.eventId;
		const eventData = {};
		this.dataInputs.forEach((port) => {
			eventData[port.name] = port.getValue(context);
		});
		context.configuration.coordinator.notifyCustomEvent(eventId, eventData);
		this.out._activateSignal(context);
	}
	/**
	* @returns class name of the block.
	*/
	getClassName() {
		return "FlowGraphReceiveCustomEventBlock";
	}
};
RegisterClass("FlowGraphReceiveCustomEventBlock", FlowGraphSendCustomEventBlock);
//#endregion
export { FlowGraphSendCustomEventBlock as t };

//# sourceMappingURL=flowGraphSendCustomEventBlock-snL16Yb6.js.map