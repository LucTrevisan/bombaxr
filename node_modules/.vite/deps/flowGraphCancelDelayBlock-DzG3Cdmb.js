import { r as RegisterClass } from "./typeStore-DHv0TG1U.js";
import { v as RichTypeNumber } from "./flowGraphBlock-Cu7z1xvN.js";
import { t as FlowGraphExecutionBlockWithOutSignal } from "./flowGraphExecutionBlockWithOutSignal-xSLd1rxK.js";
//#region node_modules/@babylonjs/core/FlowGraph/Blocks/Execution/ControlFlow/flowGraphCancelDelayBlock.js
/**
* This block cancels a delay that was previously scheduled.
*/
var FlowGraphCancelDelayBlock = class extends FlowGraphExecutionBlockWithOutSignal {
	constructor(config) {
		super(config);
		this.delayIndex = this.registerDataInput("delayIndex", RichTypeNumber);
	}
	_execute(context, _callingSignal) {
		const delayIndex = this.delayIndex.getValue(context);
		if (delayIndex <= 0 || isNaN(delayIndex) || !isFinite(delayIndex)) return this._reportError(context, "Invalid delay index");
		const timer = context._getExecutionVariable(this, "pendingDelays", [])[delayIndex];
		if (timer) timer.dispose();
		this.out._activateSignal(context);
	}
	getClassName() {
		return "FlowGraphCancelDelayBlock";
	}
};
RegisterClass("FlowGraphCancelDelayBlock", FlowGraphCancelDelayBlock);
//#endregion
export { FlowGraphCancelDelayBlock as t };

//# sourceMappingURL=flowGraphCancelDelayBlock-DzG3Cdmb.js.map