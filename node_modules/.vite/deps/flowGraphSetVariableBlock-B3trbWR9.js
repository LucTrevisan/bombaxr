import { r as RegisterClass } from "./typeStore-DHv0TG1U.js";
import { u as RichTypeAny } from "./flowGraphBlock-Cu7z1xvN.js";
import { t as FlowGraphExecutionBlockWithOutSignal } from "./flowGraphExecutionBlockWithOutSignal-xSLd1rxK.js";
//#region node_modules/@babylonjs/core/FlowGraph/Blocks/Execution/flowGraphSetVariableBlock.js
/**
* This block will set a variable on the context.
*/
var FlowGraphSetVariableBlock = class extends FlowGraphExecutionBlockWithOutSignal {
	constructor(config) {
		super(config);
		if (!config.variable && !config.variables) throw new Error("FlowGraphSetVariableBlock: variable/variables is not defined");
		if (config.variables && config.variable) throw new Error("FlowGraphSetVariableBlock: variable and variables are both defined");
		if (config.variables) for (const variable of config.variables) this.registerDataInput(variable, RichTypeAny);
		else this.registerDataInput("value", RichTypeAny);
	}
	_execute(context, _callingSignal) {
		if (this.config?.variables) for (const variable of this.config.variables) this._saveVariable(context, variable);
		else this._saveVariable(context, this.config?.variable, "value");
		this.out._activateSignal(context);
	}
	_saveVariable(context, variableName, inputName) {
		const currentlyRunningAnimationGroups = context._getGlobalContextVariable("currentlyRunningAnimationGroups", []);
		for (const animationUniqueId of currentlyRunningAnimationGroups) {
			const animation = context.assetsContext.animationGroups[animationUniqueId];
			for (const targetAnimation of animation.targetedAnimations) if (targetAnimation.target === context) {
				if (targetAnimation.target === context) {
					if (targetAnimation.animation.targetProperty === variableName) {
						animation.stop();
						const index = currentlyRunningAnimationGroups.indexOf(animationUniqueId);
						if (index > -1) currentlyRunningAnimationGroups.splice(index, 1);
						context._setGlobalContextVariable("currentlyRunningAnimationGroups", currentlyRunningAnimationGroups);
						break;
					}
				}
			}
		}
		const value = this.getDataInput(inputName || variableName)?.getValue(context);
		context.setVariable(variableName, value);
	}
	getClassName() {
		return "FlowGraphSetVariableBlock";
	}
	serialize(serializationObject) {
		super.serialize(serializationObject);
		serializationObject.config.variable = this.config?.variable;
	}
};
RegisterClass("FlowGraphSetVariableBlock", FlowGraphSetVariableBlock);
//#endregion
export { FlowGraphSetVariableBlock as t };

//# sourceMappingURL=flowGraphSetVariableBlock-B3trbWR9.js.map