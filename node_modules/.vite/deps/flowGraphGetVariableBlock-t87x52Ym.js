import { r as RegisterClass } from "./typeStore-DHv0TG1U.js";
import { t as FlowGraphBlock, u as RichTypeAny } from "./flowGraphBlock-Cu7z1xvN.js";
//#region node_modules/@babylonjs/core/FlowGraph/Blocks/Data/flowGraphGetVariableBlock.js
/**
* A block that gets the value of a variable.
* Variables are an stored in the context of the flow graph.
*/
var FlowGraphGetVariableBlock = class extends FlowGraphBlock {
	/**
	* Construct a FlowGraphGetVariableBlock.
	* @param config construction parameters
	*/
	constructor(config) {
		super(config);
		this.config = config;
		this.value = this.registerDataOutput("value", RichTypeAny, config.initialValue);
	}
	/**
	* @internal
	*/
	_updateOutputs(context) {
		const variableNameValue = this.config.variable;
		if (context.hasVariable(variableNameValue)) this.value.setValue(context.getVariable(variableNameValue), context);
	}
	/**
	* Serializes this block
	* @param serializationObject the object to serialize to
	*/
	serialize(serializationObject) {
		super.serialize(serializationObject);
		serializationObject.config.variable = this.config.variable;
	}
	getClassName() {
		return "FlowGraphGetVariableBlock";
	}
};
RegisterClass("FlowGraphGetVariableBlock", FlowGraphGetVariableBlock);
//#endregion
export { FlowGraphGetVariableBlock as t };

//# sourceMappingURL=flowGraphGetVariableBlock-t87x52Ym.js.map